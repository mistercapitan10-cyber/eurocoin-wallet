# План: Ограничение доступа к Telegram-боту для одного пользователя

## Текущая ситуация

**Проблема безопасности:** Telegram-бот сейчас доступен всем пользователям без ограничений. Любой человек, у кого есть токен бота, может:
- Просматривать все заявки на обмен (с email, суммами, адресами кошельков)
- Управлять статусами заявок
- Просматривать внутренние заявки
- Получать доступ к чатам поддержки
- Отправлять рассылки (только для MANAGER)

**Что уже есть:**
- В `.env` файле есть `TELEGRAM_ADMIN_CHAT_ID=925942852` и `TELEGRAM_MANAGER_CHAT_ID=925942852`
- Частичная проверка только для команды `/newsletter` (строка 1189-1194)
- Команда `/myid` для получения Chat ID

## Рекомендуемое решение

### Вариант 1: Проверка по User ID (РЕКОМЕНДУЕТСЯ) ⭐

**Преимущества:**
- ✅ Наиболее надежный метод
- ✅ User ID никогда не меняется
- ✅ Невозможно обойти (в отличие от username)
- ✅ Легко реализовать
- ✅ Не требует от пользователя запоминать пароли

**Как работает:**
1. Администратор получает свой Telegram User ID (командой `/myid`)
2. ID сохраняется в переменную окружения `TELEGRAM_ALLOWED_USER_ID`
3. Все команды бота проверяют `ctx.from.id === TELEGRAM_ALLOWED_USER_ID`
4. Если ID не совпадает - бот отклоняет запрос

**Что нужно изменить:**
- Добавить middleware функцию `isAuthorizedUser(ctx)` в `app/api/telegram-webhook/route.ts`
- Добавить проверку в каждый обработчик команд и callback
- Добавить переменную `TELEGRAM_ALLOWED_USER_ID` в `.env` и `.env.local`
- Обновить документацию

---

### Вариант 2: Проверка по username

**Преимущества:**
- ✅ Легко запомнить
- ✅ Можно передать доступ другому человеку, просто изменив username

**Недостатки:**
- ❌ Username можно изменить
- ❌ Не все пользователи имеют username
- ❌ Менее безопасно

---

### Вариант 3: Код доступа / Пароль

**Преимущества:**
- ✅ Можно передать несколькими людьми
- ✅ Можно отозвать доступ, изменив код

**Недостатки:**
- ❌ Неудобно для использования (нужно вводить каждый раз или при старте)
- ❌ Код может быть перехвачен
- ❌ Усложняет UX

---

## Пошаговый план реализации (Вариант 1)

### Шаг 1: Получить User ID
1. Пользователь отправляет команду `/myid` боту
2. Бот возвращает User ID (например: `925942852`)
3. Сохранить этот ID

### Шаг 2: Добавить переменную окружения

Добавить в `.env` и `.env.local`:
```env
# Разрешенный пользователь для Telegram-бота (User ID)
TELEGRAM_ALLOWED_USER_ID=925942852
```

### Шаг 3: Создать middleware функцию

В файле `app/api/telegram-webhook/route.ts` добавить:

```typescript
// Authorization middleware
function isAuthorizedUser(userId: number): boolean {
  const allowedUserId = process.env.TELEGRAM_ALLOWED_USER_ID;

  if (!allowedUserId) {
    console.warn('[telegram-webhook] TELEGRAM_ALLOWED_USER_ID is not set - bot is open to all users!');
    return true; // Fallback для разработки
  }

  return userId.toString() === allowedUserId;
}

// Helper to check access and reply if unauthorized
async function checkAccess(ctx: any): Promise<boolean> {
  const userId = ctx.from?.id;

  if (!userId || !isAuthorizedUser(userId)) {
    await ctx.reply(
      '❌ У вас нет доступа к этому боту.\n\n' +
      'Если вы администратор, обратитесь к разработчику для получения доступа.'
    );
    return false;
  }

  return true;
}
```

### Шаг 4: Добавить проверку во все обработчики

Для **каждой команды** (кроме `/myid` для получения ID):

```typescript
// Пример для команды /start
bot.start(async (ctx) => {
  if (!await checkAccess(ctx)) return; // 🔒 Проверка доступа

  // Остальной код...
});

// Пример для команды /list
bot.command("list", async (ctx) => {
  if (!await checkAccess(ctx)) return; // 🔒 Проверка доступа

  // Остальной код...
});
```

Для **всех callback handlers** (кнопки):

```typescript
// Пример для action buttons
bot.action(/^action_(.+)_(.+)$/, async (ctx) => {
  if (!await checkAccess(ctx)) {
    await ctx.answerCbQuery("❌ Нет доступа").catch(() => {});
    return;
  }

  await ctx.answerCbQuery("⏳ Обновление...").catch(() => {});
  // Остальной код...
});
```

Для **text handler** (обработка текстовых сообщений):

```typescript
bot.on("text", async (ctx) => {
  // Проверка доступа для ВСЕХ текстовых сообщений
  if (!await checkAccess(ctx)) return;

  // Остальной код...
});
```

### Шаг 5: Обновить документацию

Добавить в `CLAUDE.md` или `docs/telegram-webhook-setup.md`:

```markdown
## Безопасность бота

Бот настроен для доступа только одного пользователя.

### Настройка доступа

1. Получите ваш User ID командой `/myid`
2. Добавьте в `.env.local`:
   ```env
   TELEGRAM_ALLOWED_USER_ID=ваш_user_id
   ```
3. Перезапустите сервер

### Передача доступа другому пользователю

1. Попросите нового пользователя отправить `/myid` боту
2. Замените `TELEGRAM_ALLOWED_USER_ID` на новый ID
3. Перезапустите сервер
```

---

## Альтернативный подход: Несколько пользователей

Если в будущем нужно дать доступ нескольким людям:

```env
# Разрешенные пользователи (разделенные запятой)
TELEGRAM_ALLOWED_USER_IDS=925942852,123456789,987654321
```

```typescript
function isAuthorizedUser(userId: number): boolean {
  const allowedUserIds = process.env.TELEGRAM_ALLOWED_USER_IDS?.split(',') || [];
  return allowedUserIds.includes(userId.toString());
}
```

---

## Тестирование

### После внедрения:

1. **Тест 1: Авторизованный пользователь**
   - Отправьте `/start` от авторизованного аккаунта
   - Ожидаемый результат: Бот отвечает приветствием

2. **Тест 2: Неавторизованный пользователь**
   - Отправьте `/start` от другого аккаунта
   - Ожидаемый результат: "❌ У вас нет доступа к этому боту"

3. **Тест 3: Получение ID**
   - Отправьте `/myid` от любого аккаунта
   - Ожидаемый результат: Бот возвращает User ID (эта команда доступна всем)

4. **Тест 4: Кнопки**
   - Нажмите на inline-кнопку от неавторизованного аккаунта
   - Ожидаемый результат: "❌ Нет доступа"

---

## Итоговая оценка

### Нужно ли писать код?

**ДА**, требуется код для:
- ✅ Middleware функции `isAuthorizedUser()` и `checkAccess()`
- ✅ Добавление проверки в ~15 обработчиков команд
- ✅ Добавление проверки в ~5 callback handlers
- ✅ Добавление проверки в text handler
- ✅ Обновление переменных окружения

### Объем работы:
- **Малый** - около 50-70 строк кода
- **Время**: ~30 минут
- **Сложность**: Низкая

### Безопасность:
- ✅ Надежная защита от несанкционированного доступа
- ✅ Невозможно обойти без прямого доступа к серверу
- ✅ Легко передать доступ другому человеку

---

## Рекомендация

**Использовать Вариант 1** (проверка по User ID) - это:
- Простое и надежное решение
- Не требует сложной инфраструктуры
- Легко поддерживать и расширять
- Соответствует лучшим практикам безопасности для Telegram-ботов

**Следующий шаг:** Реализовать код согласно Шагу 2-4.
