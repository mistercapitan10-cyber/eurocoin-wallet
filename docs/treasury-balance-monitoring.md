# Мониторинг баланса казначейства

## Обзор

Система мониторинга баланса казначейства автоматически отслеживает баланс токенов на счету казначейства и отправляет уведомления админам в Telegram при достижении пороговых значений.

## Функциональность

### 1. Автоматические уведомления

Система отправляет уведомления в Telegram при следующих ситуациях:

- **Низкий баланс** — когда баланс опускается ниже порога `TREASURY_BALANCE_LOW_THRESHOLD`
- **Критически низкий баланс** — когда баланс опускается ниже порога `TREASURY_BALANCE_CRITICAL_THRESHOLD`
- **Недостаточно средств** — когда баланса недостаточно для выполнения перевода

### 2. Интеграция с процессом обработки выводов

При обработке заявок на вывод (`process-withdrawals.ts`):

- Автоматически проверяется баланс перед каждым запуском
- При недостатке баланса отправляется критическое уведомление
- Заявка автоматически отклоняется с разблокировкой баланса пользователя

### 3. Отдельный скрипт мониторинга

Скрипт `check-treasury-balance.ts` можно запускать независимо для проверки баланса:

```bash
npm run treasury:check-balance
```

## Настройка

### Переменные окружения

Добавьте следующие переменные в `.env.local` или `.env`:

```bash
# Порог низкого баланса (в токенах, не в wei)
# Уведомление отправляется, когда баланс <= этого значения
TREASURY_BALANCE_LOW_THRESHOLD=1000

# Порог критически низкого баланса (в токенах, не в wei)
# Критическое уведомление отправляется, когда баланс <= этого значения
TREASURY_BALANCE_CRITICAL_THRESHOLD=500

# Приватный ключ кошелька казначейства
TREASURY_PRIVATE_KEY=your_private_key_here

# RPC URL для подключения к блокчейну
TREASURY_RPC_URL=https://your-rpc-url.com
# или используйте
NEXT_PUBLIC_RPC_URL=https://your-rpc-url.com

# Telegram настройки (уже должны быть настроены)
TELEGRAM_API_KEY=your_bot_token
TELEGRAM_ADMIN_CHAT_ID=your_chat_id
```

### Рекомендуемые значения порогов

- **TREASURY_BALANCE_LOW_THRESHOLD**: Установите значение, которое покрывает несколько дней обычных выводов (например, 1000 токенов)
- **TREASURY_BALANCE_CRITICAL_THRESHOLD**: Установите значение, которое покрывает минимально необходимый резерв (например, 500 токенов)

## Использование

### Автоматический мониторинг

Скрипт `process-withdrawals.ts` автоматически проверяет баланс при каждом запуске. Если вы настроили cron для автоматического запуска обработки выводов, мониторинг будет работать автоматически.

Пример cron задачи (каждые 5 минут):
```bash
*/5 * * * * cd /path/to/project && npm run treasury:process
```

### Ручная проверка баланса

Запустите скрипт проверки баланса вручную:

```bash
npm run treasury:check-balance
```

### Настройка отдельного cron для мониторинга

Если вы хотите проверять баланс чаще, чем обрабатываются выводы, создайте отдельную cron задачу:

```bash
# Проверка баланса каждые 30 минут
*/30 * * * * cd /path/to/project && npm run treasury:check-balance
```

## Типы уведомлений

### 1. Низкий баланс (low)

Отправляется, когда баланс опускается ниже `TREASURY_BALANCE_LOW_THRESHOLD`:

```
⚠️ Низкий баланс казначейства

💼 Адрес казначейства:
`0x...`

💰 Текущий баланс: 800 EURC
📊 Порог: 1000 EURC
🕐 Время: 13.01.2025, 15:30:00

⚠️ Требуется пополнение баланса казначейства
```

### 2. Критически низкий баланс (critical)

Отправляется, когда баланс опускается ниже `TREASURY_BALANCE_CRITICAL_THRESHOLD`:

```
🔴 Критически низкий баланс казначейства

💼 Адрес казначейства:
`0x...`

💰 Текущий баланс: 300 EURC
📊 Порог: 500 EURC
🕐 Время: 13.01.2025, 15:30:00

⚠️ Требуется пополнение баланса казначейства
```

### 3. Недостаточно средств (insufficient)

Отправляется автоматически при попытке выполнить перевод с недостаточным балансом:

```
🚨 Недостаточно средств казначейства

💼 Адрес казначейства:
`0x...`

💰 Текущий баланс: 50 EURC
💰 Требуется: 100 EURC
🕐 Время: 13.01.2025, 15:30:00

⚠️ Требуется пополнение баланса казначейства
```

## Автоматическое пополнение

В текущей реализации система только отправляет уведомления о необходимости пополнения. Для автоматического пополнения можно:

1. **Интегрировать с внешним API** — добавить вызов API для автоматического пополнения при достижении критического порога
2. **Использовать внешний сервис** — настроить внешний сервис, который отслеживает уведомления и выполняет пополнение
3. **Ручное пополнение** — админы получают уведомления и пополняют баланс вручную

### Пример интеграции автоматического пополнения

Для добавления автоматического пополнения можно расширить скрипт `check-treasury-balance.ts`:

```typescript
// После отправки уведомления о критическом балансе
if (status === "critical") {
  // Вызов API для автоматического пополнения
  await refillTreasuryFromReserve();
}
```

## Логирование

Все операции логируются в консоль:

```
[treasury-balance-check] Treasury balance: 1200 EURC
[treasury-balance-check] Treasury address: 0x...
[treasury-balance-check] ✅ Balance is above all thresholds
```

При достижении порогов:
```
[treasury-balance-check] ⚠️ LOW: Balance 800 <= 1000
[treasury-balance-check] ⚠️ CRITICAL: Balance 300 <= 500
```

## Безопасность

- Приватный ключ казначейства хранится только в переменных окружения
- Скрипты работают в режиме DRY_RUN, если не настроены необходимые переменные
- Уведомления отправляются только авторизованным админам (через `TELEGRAM_ADMIN_CHAT_ID`)

## Устранение неполадок

### Уведомления не отправляются

1. Проверьте, что `TELEGRAM_API_KEY` и `TELEGRAM_ADMIN_CHAT_ID` настроены
2. Убедитесь, что пороги установлены (`TREASURY_BALANCE_LOW_THRESHOLD` и/или `TREASURY_BALANCE_CRITICAL_THRESHOLD`)
3. Проверьте логи на наличие ошибок

### Скрипт не запускается

1. Проверьте, что все переменные окружения установлены
2. Убедитесь, что RPC URL доступен
3. Проверьте, что токен адрес настроен (`NEXT_PUBLIC_TOKEN_ADDRESS`)

### Баланс не проверяется

1. Убедитесь, что `TREASURY_PRIVATE_KEY` установлен
2. Проверьте, что `RPC_URL` доступен и работает
3. Проверьте логи на наличие ошибок подключения

## Связанные команды

- `npm run treasury:process` — обработка заявок на вывод (включает проверку баланса)
- `npm run treasury:check-balance` — проверка баланса казначейства

