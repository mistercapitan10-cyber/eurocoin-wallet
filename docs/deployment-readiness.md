# 🚀 Готовность к деплою

## 📊 Текущая фаза разработки

**Фаза 3: Блокчейн-интеграция и продвинутые функции** (в процессе завершения)

Согласно плану разработки (`docs/requirements.md`), проект находится на **Фазе 3**, где реализуются:

- ✅ Интеграция Web3 (wagmi + viem)
- ✅ Работа с токеном ERC-20
- ✅ Система заявок на вывод средств
- ✅ Telegram-бот интеграция
- ✅ Чат-бот с AI
- ✅ OAuth аутентификация
- ✅ Мультиязычность (EN/RU)

---

## ✅ Что готово к деплою

### 1. Инфраструктура и сборка

- ✅ **Docker конфигурация для продакшена**
  - `docker/Dockerfile` - многоступенчатая сборка
  - `docker/docker-compose.prod.yml` - оркестрация сервисов
  - Health checks настроены для PostgreSQL и Redis
  - Автоматический запуск миграций при старте

- ✅ **Next.js конфигурация**
  - Next.js 16.0.0 с App Router
  - TypeScript 5.0 в strict mode
  - Оптимизация изображений настроена

- ✅ **Скрипты сборки и деплоя**
  - `npm run build` - сборка продакшен версии
  - `npm run start` - запуск продакшен сервера
  - `npm run db:init` - инициализация базы данных
  - `npm run db:migrate` - применение миграций
  - `npm run auth:migrate` - применение auth миграций

### 2. База данных

- ✅ **PostgreSQL схема**
  - Таблицы: `exchange_requests`, `internal_requests`, `investigation_stages`, `chatbot_messages`
  - Индексы для оптимизации запросов
  - Триггеры для автоматического обновления `updated_at`
  - Миграции: `add_current_stage.sql`, `add-user-id-to-requests.sql`

- ✅ **Инициализация БД**
  - Скрипт `scripts/init-database.ts` создает все таблицы
  - Автоматический запуск при Docker деплое

### 3. Аутентификация и авторизация

- ✅ **NextAuth.js v5 интеграция**
  - OAuth провайдеры: Google, GitHub
  - Email провайдер (через Resend) - опционально
  - JWT стратегия для serverless совместимости
  - Кастомные страницы входа (`/login`)
  - Middleware для защиты роутов

- ✅ **Множественные методы аутентификации**
  - MetaMask подключение (cookie-based)
  - OAuth (Google, GitHub)
  - Email magic links (опционально, только в продакшене)

### 4. Блокчейн интеграция

- ✅ **Web3 конфигурация (wagmi + viem)**
  - Поддержка сетей: Sepolia (testnet), Ethereum Mainnet
  - Конфигурация в `config/chains.ts` и `lib/wagmi.tsx`
  - Автоматическое переключение сетей

- ✅ **Работа с токеном**
  - Конфигурация токена в `config/token.ts`
  - Fallback значения для незаданных переменных
  - Поддержка ERC-20 ABI

### 5. Backend API

- ✅ **API Routes реализованы**
  - `/api/submit-request` - подача внутренних заявок
  - `/api/submit-exchange-request` - заявки на обмен
  - `/api/investigation/status` - отслеживание расследований
  - `/api/telegram-webhook` - интеграция с Telegram ботом
  - `/api/chatbot/*` - чат-бот endpoints
  - `/api/user/requests` - получение заявок пользователя

### 6. Frontend функциональность

- ✅ **Основные страницы**
  - Главная страница с дашбордом
  - Страница профиля пользователя
  - Страница обмена (exchange)
  - Страница логина
  - Информационные страницы

- ✅ **KCомпоненты**
  - Wallet компоненты (подключение, баланс, статистика)
  - Exchange компоненты (калькулятор, форма заявки)
  - Chatbot компоненты (чат виджет, сообщения)
  - Dashboard компоненты (прогресс расследований)
  - Layout компоненты (header, footer, navigation)

- ✅ **Интернационализация**
  - Поддержка EN/RU языков
  - Централизованные переводы в `lib/i18n/translations.ts`
  - Переключатель языка в интерфейсе

### 7. Интеграции

- ✅ **Telegram бот**
  - Настроен webhook endpoint
  - Уведомления менеджеру о заявках
  - Обра даль сообщений от пользователей

- ✅ **Email сервис (Resend)**
  - Настроен для отправки уведомлений
  - Email аутентификация (опционально)

### 8. Безопасность (частично)

- ✅ **Аутентификация и валидация**
  - Middleware защита роутов
  - Валидация входных данных в API routes
  - Защита cookies (httpOnly, secure в продакшене)

- ✅ **Секреты**
  - Переменные окружения для всех ключей
  - Никакого хранения приватных ключей

---

## ⚠️ Что нужно настроить перед деплоем

### 🔴 Критически важно

#### 1. Security Headers в Next.js

**Проблема:** В `next.config.ts` отсутствуют security headers, требуемые архитектурой.

**Решение:** Добавить security headers:

```typescript
// next.config.ts
const securityHeaders = [
  {
    key: "Content-Security-Policy",
    value:
      "default-src 'self'; script-src 'self' 'unsafe-eval' 'unsafe-inline' https://*.wagmi.sh https://*.viem.sh; connect-src 'self' https://*.infura.io https://*.alchemyapi.io https://*.etherscan.io https://*.coingecko.com wss://*.infura.io wss://*.alchemyapi.io;",
  },
  {
    key: "X-Frame-Options",
    value: "DENY",
  },
  {
    key: "X-Content-Type-Options",
    value: "nosniff",
  },
  {
    key: "Referrer-Policy",
    value: "strict-origin-when-cross-origin",
  },
  {
    key: "Permissions-Policy",
    value: "camera=(), microphone=(), geolocation=()",
  },
];

const nextConfig: NextConfig = {
  // ... existing config
  async headers() {
    return [
      {
        source: "/:path*",
        headers: securityHeaders,
      },
    ];
  },
};
```

#### 2. Переменные окружения для продакшена

**Требуется создать/проверить все переменные:**

```env
# Blockchain Configuration (ОБЯЗАТЕЛЬНО)
NEXT_PUBLIC_TOKEN_ADDRESS=0x88F43B9f5A6d4ADEF8f80D646732F5b6153C2586
NEXT_PUBLIC_TOKEN_SYMBOL=EURC
NEXT_PUBLIC_TOKEN_DECIMALS=18
NEXT_PUBLIC_TOKEN_CHAIN_ID= # 1 для Mainnet, 11155111 для Sepolia

# RPC URLs (ОБЯЗАТЕЛЬНО - получить ключи)
NEXT_PUBLIC_RPC_URL_SEPOLIA=https://sepolia.infura.io/v3/YOUR_KEY
NEXT_PUBLIC_RPC_URL_MAINNET=https://mainnet.infura.io/v3/YOUR_KEY

# Pricing Configuration
NEXT_PUBLIC_PRICE_SOURCE=coingecko
NEXT_PUBLIC_COINGECKO_TOKEN_ID=euro-coin
NEXT_PUBLIC_TOKEN_PRICE_USD=1.00

# Exchange Configuration
NEXT_PUBLIC_EXCHANGE_RATE_RUB_PER_TOKEN=150
NEXT_PUBLIC_EXCHANGE_COMMISSION_PERCENT=1.5

# Database (ОБЯЗАТЕЛЬНО)
DATABASE_URL=postgresql://user:password@host:5432/web_wallet_db

# OAuth Configuration (ОБЯЗАТЕЛЬНО для OAuth)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# NextAuth (ОБЯЗАТЕЛЬНО)
AUTH_SECRET=generate_with: openssl rand -base64 32
NEXTAUTH_URL=https://your-domain.com

# Telegram Bot (ОБЯЗАТЕЛЬНО для Telegram функций)
TELEGRAM_API_KEY=your_telegram_bot_token
TELEGRAM_MANAGER_CHAT_ID=your_telegram_chat_id

# Email Service (ОБЯЗАТЕЛЬНО для email аутентификации)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx
SENDER_EMAIL=noreply@yourdomain.com
RECIPIENT_EMAIL=support@yourdomain.com

# App Configuration
NEXT_PUBLIC_APP_NAME=EuroCoin Wallet
NODE_ENV=production

# Email Auth (опционально)
ENABLE_EMAIL_AUTH=true  # установить в true для включения email auth
```

**Действия:**

- [ ] Получить RPC ключи от Infura или Alchemy
- [ ] Создать OAuth приложения в Google Cloud Console и GitHub
- [ ] Сгенерировать `AUTH_SECRET` для NextAuth
- [ ] Настроить Telegram бота через BotFather
- [ ] Получить Resend API ключ и настроить домен
- [ ] Подготовить продакшен базу данных PostgreSQL

#### 3. База данных - включить Drizzle Adapter

**Проблема:** В `lib/auth.ts` adapter отключен (JWT-only mode). Пользователи не сохраняются в БД.

**Решение:** Раскомментировать и настроить adapter:

```typescript
// lib/auth.ts
import { db } from "@/lib/database/drizzle";
import { DrizzleAdapter } from "@auth/drizzle-adapter";

const adapter = DrizzleAdapter(db);
```

**Также проверить:**

- [ ] Миграция auth таблиц применена (`npm run auth:migrate`)
- [ ] Таблицы `users`, `accounts`, `sessions`, `verification_tokens` существуют

#### 4. Настройка Telegram Webhook

**Для продакшена нужно:**

- [ ] Установить webhook URL: `https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://your-domain.com/api/telegram-webhook`
- [ ] Проверить, что webhook работает корректно

#### 5. SSL сертификат

**Для продакшена:**

- [ ] Убедиться, что домен имеет SSL сертификат (автоматически на Vercel)
- [ ] Проверить, что все запросы идут через HTTPS

### 🟡 Важно (желательно перед деплоем)

#### 6. Тестирование на production-like окружении

- [ ] Протестировать полный флоу подключения кошелька
- [ ] Протестировать OAuth вход (Google, GitHub)
- [ ] Протестировать создание заявок
- [ ] Протестировать Telegram интеграцию
- [ ] Протестировать чат-бот функциональность
- [ ] Протестировать на мобильных устройствах

#### 7. Мониторинг и логирование

**Рекомендуется добавить:**

- [ ] Настроить логирование ошибок (Sentry, LogRocket, или Vercel Analytics)
- [ ] Настроить мониторинг базы данных
- [ ] Настроить алерты для критических ошибок

#### 8. Rate Limiting

**Для защиты API endpoints:**

- [ ] Добавить rate limiting для API routes
- [ ] Особенно важно для `/api/submit-request` и `/api/telegram-webhook`

#### 9. Резервное копирование базы данных

- [ ] Настроить автоматические бэкапы PostgreSQL
- [ ] Протестировать процедуру восстановления

#### 10. Проверка производительности

- [ ] Проверить размер bundle (Next.js автоматически оптимизирует)
- [ ] Проверить время загрузки страниц (Lighthouse)
- [ ] Оптимизировать изображения (уже настроено в next.config.ts)

### 🟢 Опционально (можно после деплоя)

#### 11. Analytics

- [ ] Настроить аналитику (Google Analytics, Vercel Analytics)
- [ ] Настроить отслеживание конверсий

#### 12. Error Boundaries

- [ ] Добавить React Error Boundaries для graceful error handling

#### 13. Дополнительная документация

- [ ] Обновить README с production инструкциями
- [ ] Создать runbook для операций

---

## 📋 Чеклист перед деплоем

### Инфраструктура

- [ ] Настроить продакшен базу данных PostgreSQL
- [ ] Настроить Redis (если используется для кеширования)
- [ ] Подготовить домен и DNS настройки
- [ ] Настроить CDN (опционально, Vercel включает)

### Конфигурация

- [ ] Все переменные окружения заполнены и проверены
- [ ] Security headers добавлены в `next.config.ts`
- [ ] Drizzle Adapter включен в `lib/auth.ts`
- [ ] Telegram webhook настроен
- [ ] OAuth приложения созданы и настроены

### База данных

- [ ] Миграции применены в продакшен БД
- [ ] Auth таблицы созданы
- [ ] Тестовые данные удалены (если были)
- [ ] Бэкапы настроены

### Тестирование

- [ ] Все тесты пройдены (если есть)
- [ ] Ручное тестирование критических функций
- [ ] Тестирование на разных браузерах
- [ ] Тестирование на мобильных устройствах
- [ ] Тестирование на testnet (Sepolia)

### Безопасность

- [ ] Security headers настроены
- [ ] HTTPS включен
- [ ] Секреты не закоммичены в git
- [ ] Rate limiting настроен
- [ ] Валидация всех входных данных работает

### Мониторинг

- [ ] Логирование настроено
- [ ] Мониторинг настроен
- [ ] Алерты настроены

---

## 🚀 План деплоя

### Вариант 1: Vercel (Рекомендуется)

1. **Подготовка:**

   ```bash
   # Убедиться, что код готов
   npm run build
   npm run lint
   npm run type-check
   ```

2. **Деплой:**

   ```bash
   # Установить Vercel CLI
   npm i -g vercel

   # Деплой
   vercel --prod
   ```

3. **Настройка в Vercel Dashboard:**
   - [ ] Добавить все переменные окружения
   - [ ] Настроить PostgreSQL через Vercel Postgres или внешний провайдер
   - [ ] Настроить домен
   - [ ] Включить HTTPS

4. **Post-deploy:**
   - [ ] Запустить миграции: `vercel env pull && npm run db:migrate`
   - [ ] Проверить работу приложения
   - [ ] Настроить Telegram webhook на новый URL

### Вариант 2: Docker (Собственный сервер)

1. **Подготовка сервера:**

   ```bash
   # На сервере
   git clone <repository>
   cd web-wallet
   ```

2. **Создать .env файл** с всеми переменными окружения

3. **Запустить:**

   ```bash
   docker compose -f docker/docker-compose.prod.yml up -d --build
   ```

4. **Проверить логи:**

   ```bash
   docker compose -f docker/docker-compose.prod.yml logs -f
   ```

5. **Настроить Nginx reverse proxy** (опционально):
   - SSL сертификат через Let's Encrypt
   - Proxy на порт 3000

---

## 📊 Текущий статус готовности

| Категория          | Готовность | Статус                                      |
| ------------------ | ---------- | ------------------------------------------- |
| **Инфраструктура** | 90%        | ✅ Docker готов, нужны переменные окружения |
| **База данных**    | 85%        | ✅ Схема готова, нужно включить adapter     |
| **Frontend**       | 95%        | ✅ Все компоненты готовы                    |
| **Backend API**    | 90%        | ✅ Все endpoints реализованы                |
| **Аутентификация** | 85%        | ✅ OAuth работает, нужен adapter            |
| **Безопасность**   | 70%        | ⚠️ Нужны security headers                   |
| **Интеграции**     | 90%        | ✅ Telegram, Email готовы, нужны ключи      |
| **Документация**   | 80%        | ✅ Хорошая документация, можно дополнить    |

**Общая готовность: ~85%**

---

## ⏱️ Примерное время на подготовку к деплою

| Задача                                     | Время         |
| ------------------------------------------ | ------------- |
| Настройка security headers                 | 15 минут      |
| Получение и настройка переменных окружения | 2-3 часа      |
| Включение Drizzle Adapter                  | 30 минут      |
| Тестирование на production-like окружении  | 2-3 часа      |
| Настройка мониторинга (базовая)            | 1 час         |
| Настройка бэкапов БД                       | 30 минут      |
| **Итого**                                  | **6-8 часов** |

---

## 📝 Резюме

Проект находится в хорошем состоянии для деплоя. Основные функции реализованы, инфраструктура подготовлена.

**Главные задачи перед деплоем:**

1. Добавить security headers
2. Настроить все переменные окружения
3. Включить Drizzle Adapter для сохранения пользователей
4. Протестировать на production-like окружении

После выполнения этих задач проект будет готов к продакшен деплою.

---

**Последнее обновление:** 30 октября 2025  
**Версия документа:** 1.0
