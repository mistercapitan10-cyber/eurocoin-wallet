# Анализ готовности системы возврата средств

**Дата анализа:** 2025-01-27  
**Основание:** Требования к функционалу системы возврата средств

---

## 📋 Описание требуемой логики

1. **Регистрация пользователя**
2. **Заявление на возврат** (ФИО, сумма потерянных средств, чеки подтверждения)
   - Пользователь указывает сумму, которую потерял у брокеров
   - Подтверждает документами/чеками
3. **Обработка заявления на сайте**
4. **Появление суммы на балансе на сайте**
   - ⚠️ **ВАЖНО:** Администратор **ВРУЧНУЮ** добавляет баланс на сайте после проверки документов
   - Сумма соответствует указанной пользователем сумме потерянных средств
   - Автоматическое начисление НЕ требуется
5. **Вывод средств с баланса на кошелек**
   - Вывод происходит **БЕЗ комиссии** (чтобы показать, что вывод реальный)
   - Токены переводятся на MetaMask пользователя
   - Токены без стоимости, но желательно отображать как со стоимостью (визуально)
6. **Комиссия за услуги**
   - ⚠️ **ВАЖНО:** Комиссия берется **ПОСЛЕ** успешного вывода
   - Комиссия взимается за проделанную работу по возврату средств
   - Оплата комиссии: через ТГ бот или на сайте с вшитым кошельком, или сообщение о связи с поддержкой/обменником

---

## ✅ Что уже реализовано

### 1. Регистрация пользователя

**Статус:** ✅ **ГОТОВО**

**Что реализовано:**

- ✅ Регистрация через MetaMask (wallet connection)
- ✅ Регистрация через OAuth (Google, GitHub)
- ✅ Аутентификация через NextAuth
- ✅ Middleware для защиты страниц
- ✅ Хранение пользователей в базе данных (через OAuth)
- ✅ Связь заявок с пользователем (`user_id` в таблицах заявок)

**Файлы:**

- `app/login/page.tsx` - страница входа
- `components/auth/` - компоненты аутентификации
- `lib/auth.ts` - логика аутентификации
- `middleware.ts` - проверка авторизации
- `app/api/auth/[...nextauth]/route.ts` - OAuth провайдеры

**Что работает:**

- Пользователь может зарегистрироваться через MetaMask или OAuth
- После регистрации доступен `userId` и `email`
- Заявки могут быть привязаны к пользователю

---

### 2. Заявление на возврат

**Статус:** ⚠️ **ЧАСТИЧНО ГОТОВО**

**Что реализовано:**

- ✅ Форма подачи заявки (`components/forms/internal-request-form.tsx`)
- ✅ API endpoint для приема заявок (`app/api/submit-request/route.ts`)
- ✅ Сохранение заявок в базу данных (таблица `internal_requests`)
- ✅ Отправка уведомлений на email

**Что отсутствует:**

- ❌ Поле "ФИО" (есть только `requester` - имя заявителя, но не полное ФИО)
- ❌ Поле "Сумма потерянных средств" - такого поля нет в форме
- ❌ Поле "Чеки подтверждения" - нет функционала загрузки файлов/чеков

**Текущая структура заявки:**

```typescript
{
  requester: string,        // Имя заявителя (не полное ФИО)
  department: string,       // Отдел
  requestType: string,      // Тип заявки
  priority: string,         // Приоритет
  description: string,      // Описание (текстовое поле)
  walletAddress?: string,   // Адрес кошелька (опционально)
  email?: string,           // Email
  userId?: string           // ID пользователя (OAuth)
}
```

**Что нужно доработать:**

1. Добавить поле "Полное ФИО" (или разделить на Фамилия/Имя/Отчество)
2. Добавить поле "Сумма потерянных средств" (number/string)
3. Добавить функционал загрузки файлов (чеки, скриншоты, документы)
4. Возможно, создать отдельный тип заявки "Возврат средств" (refund)

**Файлы для доработки:**

- `components/forms/internal-request-form.tsx`
- `lib/database/schema.sql` (добавить поля в таблицу `internal_requests`)
- `app/api/submit-request/route.ts`

---

### 3. Обработка заявления на сайте

**Статус:** ✅ **ГОТОВО**

**Что реализовано:**

- ✅ Сохранение заявок в базу данных
- ✅ Отслеживание статусов заявок (`pending`, `processing`, `completed`, `rejected`)
- ✅ Отслеживание этапов обработки (`current_stage`)
- ✅ API для получения статуса обработки (`app/api/investigation/status/route.ts`)
- ✅ Визуализация прогресса обработки (`components/dashboard/investigation-progress.tsx`)
- ✅ Обновление статусов через Telegram webhook (`app/api/telegram-webhook/route.ts`)
- ✅ Обновление статусов через API (`app/api/webhook/update-request/route.ts`)

**Этапы обработки:**

1. Заявка подана (submitted)
2. Проверка документов (checking)
3. Анализ транзакций (analyzing)
4. Расследование (investigating)
5. Восстановление средств (recovering)
6. Завершено (completed)

**Что работает:**

- Пользователь видит прогресс обработки заявки на главной странице
- Администратор может менять статусы через Telegram бота
- Статусы обновляются в реальном времени

---

### 4. Баланс на сайте

**Статус:** ❌ **НЕ РЕАЛИЗОВАНО**

**Что есть сейчас:**

- ✅ Компонент отображения баланса (`components/wallet/balance-card.tsx`)
- ✅ Hook для получения баланса токенов из блокчейна (`hooks/use-token-balance.ts`)

**Проблема:**

- Текущий баланс показывает **только баланс токенов в блокчейне** (из смарт-контракта)
- Нет концепции **внутреннего баланса на сайте**, который пополняется после одобрения заявки

**Что нужно реализовать:**

1. Создать таблицу `user_balances` в базе данных:

   ```sql
   CREATE TABLE user_balances (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     user_id VARCHAR(255),              -- ID пользователя (OAuth) или NULL
     wallet_address VARCHAR(255),       -- Адрес кошелька (MetaMask) или NULL
     email VARCHAR(255),                -- Email пользователя
     balance DECIMAL(20, 8) DEFAULT 0,  -- Внутренний баланс
     currency VARCHAR(10) DEFAULT 'TOKEN',
     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
     updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
   );
   ```

2. API для управления балансом:
   - `POST /api/balance/add` - **РУЧНОЕ** начисление баланса администратором (после проверки документов)
   - `GET /api/balance` - получение баланса пользователя
   - `POST /api/balance/subtract` - списание баланса (при выводе)
   - `GET /api/admin/balance` - админский endpoint для начисления баланса (с авторизацией)

3. Компонент для отображения внутреннего баланса:
   - Создать новый компонент `internal-balance-card.tsx`
   - Или доработать существующий `balance-card.tsx` для показа двух балансов:
     - Баланс в блокчейне (токены)
     - Внутренний баланс на сайте (для возврата)
   - Показывать сумму, которую администратор вручную добавил после проверки

4. **РУЧНОЕ начисление баланса администратором:**
   - ⚠️ **ВАЖНО:** Начисление НЕ автоматическое
   - Администратор проверяет заявку и документы
   - После подтверждения, администратор через админ-панель или Telegram бота добавляет баланс
   - Сумма берется из поля "Сумма потерянных средств" заявки
   - Можно добавить команду в Telegram бот: `/add_balance <request_id>` для удобства

---

### 5. Вывод средств с баланса на кошелек

**Статус:** ❌ **НЕ РЕАЛИЗОВАНО**

**Что отсутствует:**

- ❌ Функционал вывода средств с внутреннего баланса
- ❌ API для обработки вывода
- ❌ UI для инициации вывода
- ❌ Интеграция с блокчейном для отправки токенов (без стоимости)
- ❌ Отображение токенов как со стоимостью (визуально)

**Что нужно реализовать:**

1. Страница/компонент вывода средств:
   - Показывать доступный внутренний баланс
   - Поле для ввода суммы вывода
   - Поле для выбора адреса кошелька
   - Кнопка "Вывести"

2. API endpoint для вывода:

   ```
   POST /api/withdraw
   {
     amount: string,
     walletAddress: string,
     userId?: string,
     email?: string
   }
   ```

3. Логика вывода:
   - ⚠️ **ВАЖНО:** Вывод происходит **БЕЗ комиссии** (чтобы показать, что вывод реальный)
   - Проверить достаточность баланса
   - Вычесть полную сумму из внутреннего баланса (без списания комиссии)
   - Отправить токены на указанный кошелек (MetaMask пользователя)
   - Сохранить транзакцию в историю
   - Токены без стоимости, но визуально отображать как токены со стоимостью
   - После успешного вывода, создать запись о необходимости оплаты комиссии

4. Интеграция с блокчейном:
   - Использовать `wagmi` для отправки транзакций
   - Или использовать серверный кошелек для массовых выплат
   - Токены должны быть специального типа (без стоимости) или обычные токены с нулевой стоимостью

5. История выводов:
   - Таблица `withdrawals` в БД
   - Отображение истории в профиле пользователя

**Особенность:**

- Токены не несут стоимость, но желательно отображать как со стоимостью
- Это означает, что нужно:
  - Визуально показывать эквивалент в USD
  - Но токены на самом деле не имеют стоимости на рынке
  - Можно использовать фиксированный курс для отображения

---

### 6. Комиссия за услуги (оплата после вывода)

**Статус:** ❌ **НЕ РЕАЛИЗОВАНО**

**Логика работы:**

- ⚠️ **ВАЖНО:** Комиссия берется **ПОСЛЕ** успешного вывода средств
- После того, как пользователь получил токены на MetaMask (без комиссии)
- Система создает запись о необходимости оплаты комиссии
- Комиссия взимается за проделанную работу по возврату средств

**Что есть сейчас:**

- ✅ Telegram бот для обмена (`app/api/telegram-webhook/route.ts`)
- ✅ Система обмена токенов (`components/exchange/exchange-section.tsx`)
- ✅ Заявки на обмен (`app/api/submit-exchange-request/route.ts`)

**Что отсутствует:**

- ❌ Система отслеживания неоплаченных комиссий
- ❌ Таблица `service_fees` или поле в `withdrawals` для комиссий
- ❌ UI для отображения суммы комиссии пользователю
- ❌ Уведомления о необходимости оплаты комиссии
- ❌ Вшитый кошелек для оплаты на сайте
- ❌ Компонент оплаты комиссий
- ❌ Интеграция с платежными системами
- ❌ Информационное сообщение о связи с поддержкой/обменником

**Что нужно реализовать:**

1. Добавить поле `service_fee` в таблицу `withdrawals`:
   - Сумма комиссии
   - Статус оплаты (`unpaid`, `paid`, `pending`)
   - Дата оплаты

2. Логика создания записи о комиссии:
   - После успешного вывода, автоматически создать запись о комиссии
   - Комиссия рассчитывается от суммы вывода (процент или фиксированная сумма)
   - Показать пользователю сумму комиссии и статус оплаты

3. UI для отображения комиссии:
   - Компонент в профиле: "Неоплаченные комиссии"
   - История выводов с указанием статуса комиссии
   - Уведомление, если есть неоплаченные комиссии

4. Варианты оплаты комиссии:

   **Вариант 1: Оплата через сайт (сложно, долго внедрять)**
   - Интеграция платежных систем (Stripe, PayPal, криптоплатежи)
   - Вшитый кошелек для приема платежей
   - Обработка платежей и обновление статуса
   - Время разработки: ~2-3 недели

   **Вариант 2: Оплата через Telegram бот (проще)**
   - Уже есть Telegram бот
   - Команда `/pay_fee <withdrawal_id>` для получения реквизитов
   - Бот предоставляет реквизиты для оплаты
   - После оплаты администратор обновляет статус вручную или автоматически
   - Время разработки: ~4-6 часов

   **Вариант 3: Информационное сообщение (самое простое)**
   - Добавить раздел "Оплата комиссии" на сайте
   - Показать сумму комиссии и информацию: "Для оплаты комиссии свяжитесь с отделом поддержки или нашим обменником"
   - Контакты поддержки и обменника
   - Время разработки: ~1-2 часа

**Рекомендация:**

- Если нужно быстро: **Вариант 3** (информационное сообщение) + отслеживание неоплаченных комиссий
- Если нужно в будущем: **Вариант 2** (расширить Telegram бота)
- Если критично важно: **Вариант 1** (полная интеграция на сайте)

---

## 📊 Сводная таблица готовности

| Функционал                  | Статус            | Готовность | Приоритет |
| --------------------------- | ----------------- | ---------- | --------- |
| 1. Регистрация пользователя | ✅ Готово         | 100%       | -         |
| 2. Заявление на возврат     | ⚠️ Частично       | 40%        | Высокий   |
| 3. Обработка заявления      | ✅ Готово         | 100%       | -         |
| 4. Баланс на сайте          | ❌ Не реализовано | 0%         | Высокий   |
| 5. Вывод на кошелек         | ❌ Не реализовано | 0%         | Высокий   |
| 6. Комиссия за услуги       | ❌ Не реализовано | 0%         | Средний   |

---

## 🔧 План доработки

### Приоритет 1: Критичные функции

#### 1.1 Доработка формы заявления на возврат

**Время:** 4-6 часов

- [ ] Добавить поле "Полное ФИО" (или Фамилия/Имя/Отчество)
- [ ] Добавить поле "Сумма потерянных средств" (числовое)
- [ ] Добавить функционал загрузки файлов (чеки, документы)
- [ ] Создать миграцию БД для новых полей
- [ ] Обновить API для обработки новых полей

**Файлы:**

- `components/forms/internal-request-form.tsx`
- `lib/database/schema.sql` (миграция)
- `lib/database/migrations/add-refund-fields.sql`
- `app/api/submit-request/route.ts`

---

#### 1.2 Система внутреннего баланса

**Время:** 8-12 часов

- [ ] Создать таблицу `user_balances` в БД
- [ ] Создать API endpoints для управления балансом (GET для пользователя, POST для админа)
- [ ] Создать **админский API endpoint** для ручного начисления баланса
- [ ] Создать компонент отображения внутреннего баланса
- [ ] ⚠️ **НЕ** реализовывать автоматическое начисление - только ручное через админа
- [ ] Добавить команду в Telegram бот для начисления баланса (`/add_balance <request_id> <amount>`)
- [ ] Интегрировать с системой заявок (показывать связанные заявки)

**Файлы:**

- `lib/database/schema.sql` (новая таблица)
- `lib/database/queries.ts` (функции для работы с балансом)
- `app/api/balance/route.ts` (GET для пользователя)
- `app/api/admin/balance/route.ts` (POST для админа - ручное начисление)
- `components/wallet/internal-balance-card.tsx` (новый компонент)
- `app/api/telegram-webhook/route.ts` (добавить команду для начисления баланса)

---

#### 1.3 Функционал вывода средств

**Время:** 12-16 часов

- [ ] Создать страницу/компонент вывода средств
- [ ] Создать API endpoint для обработки вывода
- [ ] ⚠️ Реализовать вывод **БЕЗ комиссии** (полная сумма)
- [ ] Реализовать проверку баланса и списание полной суммы
- [ ] Интегрировать с блокчейном для отправки токенов на MetaMask
- [ ] Создать таблицу `withdrawals` для истории (с полем `service_fee` и `fee_status`)
- [ ] После успешного вывода автоматически создать запись о комиссии
- [ ] Реализовать визуальное отображение токенов как со стоимостью
- [ ] Добавить историю выводов в профиль с указанием статуса комиссии

**Файлы:**

- `app/withdraw/page.tsx` (новая страница)
- `components/wallet/withdraw-form.tsx` (компонент формы)
- `app/api/withdraw/route.ts` (API endpoint - вывод без комиссии)
- `lib/database/schema.sql` (таблица withdrawals с полями для комиссии)
- `lib/database/queries.ts` (функции для выводов и комиссий)
- `components/profile/withdraw-history.tsx` (история выводов с комиссией)

---

### Приоритет 2: Дополнительные функции

#### 2.1 Система комиссий (оплата после вывода)

**Время:** 4-6 часов (базовая версия) или 2-3 недели (полная интеграция)

**Базовые задачи (обязательные):**

- [ ] Добавить поля `service_fee` и `fee_status` в таблицу `withdrawals`
- [ ] Создать функцию расчета комиссии (процент или фиксированная)
- [ ] После успешного вывода автоматически создать запись о комиссии
- [ ] Компонент для отображения неоплаченных комиссий в профиле
- [ ] Уведомления о необходимости оплаты комиссии

**Вариант A: Информационное сообщение (быстро) - 2 часа**

- [ ] Создать страницу/раздел "Оплата комиссии"
- [ ] Показать список неоплаченных комиссий
- [ ] Добавить информацию о контактах поддержки и обменника
- [ ] Добавить ссылки на Telegram бота

**Вариант B: Расширение Telegram бота (средний срок) - 4-6 часов**

- [ ] Добавить команды для оплаты в бота (`/pay_fee <withdrawal_id>`)
- [ ] Реализовать обработку запросов на оплату
- [ ] Бот предоставляет реквизиты для оплаты
- [ ] После оплаты администратор обновляет статус

**Вариант C: Полная интеграция на сайте (долго) - 2-3 недели**

- [ ] Выбрать платежную систему
- [ ] Интегрировать платежный шлюз
- [ ] Реализовать обработку платежей
- [ ] Создать UI для оплаты комиссии
- [ ] Автоматическое обновление статуса после оплаты
- [ ] Тестирование и отладка

---

## 📝 Дополнительные замечания

### Вопросы для уточнения:

1. **Токены без стоимости:**
   - Это новые токены специального типа?
   - Или существующие токены с нулевой стоимостью?
   - Как именно они должны работать на блокчейне?

2. **Визуальное отображение стоимости:**
   - Какой курс использовать для отображения? (фиксированный или динамический?)
   - Откуда брать курс для отображения?

3. **Начисление баланса:**
   - ✅ **РЕШЕНО:** Администратор **ВРУЧНУЮ** начисляет баланс после проверки документов
   - НЕ требуется автоматическое начисление
   - Нужен админский интерфейс или команда в Telegram боте для начисления

4. **Комиссия за услуги:**
   - ✅ **РЕШЕНО:** Комиссия берется **ПОСЛЕ** успешного вывода (без комиссии)
   - Вывод происходит полной суммой без списания комиссии
   - После вывода создается запись о необходимости оплаты комиссии
   - Какой вариант оплаты предпочтительнее? (быстрый/средний/полный)
   - Какой процент или фиксированная сумма комиссии?

5. **Загрузка файлов (чеки):**
   - Где хранить файлы? (локально, S3, другая система)
   - Максимальный размер файлов?
   - Поддерживаемые форматы?

---

## 🎯 Итоговая оценка

**Общая готовность системы:** ~35%

**Критичные пробелы:**

1. Нет внутреннего баланса на сайте (0%)
2. Нет функционала вывода средств (0%)
3. Форма заявления не содержит всех необходимых полей (40%)
4. Нет системы комиссий (оплата после вывода) (0%)
5. Нет админского функционала для ручного начисления баланса (0%)

**Что работает хорошо:**

1. Регистрация и аутентификация (100%)
2. Обработка заявок и отслеживание статусов (100%)
3. Базовая инфраструктура (БД, API, UI компоненты)
4. Telegram бот для управления заявками (100%)

**Уточненная логика работы:**

1. ✅ Пользователь заполняет заявку с ФИО, суммой потерянных средств, чеками
2. ✅ Заявка проходит обработку на сайте
3. ❌ Администратор **ВРУЧНУЮ** добавляет баланс на сайте (после проверки)
4. ❌ Пользователь выводит средства на MetaMask **БЕЗ комиссии** (чтобы показать, что вывод реальный)
5. ❌ После успешного вывода создается запись о комиссии
6. ❌ Пользователь оплачивает комиссию через поддержку/обменник/Telegram бота

**Следующие шаги:**

1. Доработать форму заявления (добавить ФИО, сумму, загрузку чеков)
2. Реализовать систему внутреннего баланса
3. Реализовать **админский функционал** для ручного начисления баланса
4. Реализовать функционал вывода средств **БЕЗ комиссии**
5. Реализовать систему комиссий (создание записи после вывода)
6. Добавить информацию об оплате комиссии (или реализовать интеграцию)

**Ориентировочное время до полной готовности:** 28-40 часов разработки

**Изменения в плане:**

- ❌ Убрать автоматическое начисление баланса
- ✅ Добавить админский функционал для ручного начисления
- ✅ Вывод без комиссии (полная сумма)
- ✅ Система комиссий после вывода
