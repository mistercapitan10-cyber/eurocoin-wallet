# 🧪 Руководство по тестированию в production-like окружении

## 📋 Обзор

Это руководство описывает процесс тестирования приложения в окружении, максимально приближенном к production, перед финальным деплоем.

**Цель:** Убедиться, что все функции работают корректно в production-режиме с оптимизацией и реальной базой данных.

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Остановить dev окружение (если запущено)
docker compose -f docker/docker-compose.dev.yml down

# Запустить production-like окружение
docker compose -f docker/docker-compose.prod.yml up --build

# Приложение будет доступно на http://localhost:3000
# Все миграции БД будут применены автоматически
```

> **Примечание:** Производственный Dockerfile теперь включает `tsx`, `lib/`, и `scripts/` для запуска миграций БД в production.

### Вариант 2: Локальная сборка

```bash
# Собрать production сборку
npm run build

# Запустить production сервер
npm run start

# Приложение будет доступно на http://localhost:3000
```

---

## 📝 Чеклист тестирования

### 1. Проверка инфраструктуры

#### 1.1 Database

- [ ] **Проверить, что все миграции применены:**

  ```bash
  docker compose -f docker/docker-compose.prod.yml exec web npm run db:test-current-stage
  ```

- [ ] **Проверить наличие всех таблиц:**

  ```bash
  docker compose -f docker/docker-compose.prod.yml exec postgres psql -U postgres -d web_wallet_db -c "\dt"
  ```

  Должны быть:
  - `exchange_requests`
  - `internal_requests`
  - `chatbot_sessions`
  - `chatbot_messages`
  - `support_messages`
  - `typing_indicators`
  - `users`
  - `accounts`
  - `sessions`
  - `verification_tokens`

- [ ] **Проверить логи БД на ошибки:**
  ```bash
  docker compose -f docker/docker-compose.prod.yml logs postgres | grep -i "error"
  ```

#### 1.2 Security Headers

- [ ] **Проверить HTTP headers:**

  ```bash
  curl -I http://localhost:3000
  ```

  Должны присутствовать:
  - `Content-Security-Policy`
  - `X-Frame-Options: DENY`
  - `X-Content-Type-Options: nosniff`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Strict-Transport-Security`

- [ ] **Проверить CSP в браузере:**
  - Открыть DevTools → Console
  - Убедиться, что нет CSP violations

#### 1.3 Environment Variables

- [ ] **Проверить, что все переменные окружения установлены:**
  ```bash
  docker compose -f docker/docker-compose.prod.yml exec web env | grep -E "NEXT_PUBLIC_|DATABASE_|TELEGRAM_|AUTH_"
  ```

---

### 2. Тестирование аутентификации

#### 2.1 Wallet Connection (MetaMask)

- [ ] **Подключение кошелька:**
  1. Открыть `http://localhost:3000`
  2. Нажать "Connect Wallet"
  3. Подтвердить в MetaMask
  4. Проверить, что адрес отображается на главной странице

- [ ] **Проверка баланса:**
  - [ ] Баланс токенов отображается
  - [ ] Эквивалент в USD отображается
  - [ ] Комиссия (Tax) отображается

- [ ] **Отключение кошелька:**
  - [ ] Кнопка "Disconnect" работает
  - [ ] После отключения пользователь перенаправляется на `/login`

#### 2.2 OAuth Authentication

- [ ] **Вход через Google:**
  1. Нажать "Sign in with Google"
  2. Выбрать аккаунт Google
  3. Проверить, что пользователь авторизован
  4. Проверить в DevTools, что cookie `__Secure-next-auth.session-token` установлен

- [ ] **Вход через GitHub:**
  1. Нажать "Sign in with GitHub"
  2. Авторизовать приложение
  3. Проверить, что пользователь авторизован
  4. Проверить cookie

- [ ] **Email вход** (если включен):
  1. Ввести email адрес
  2. Проверить, что письмо получено
  3. Нажать ссылку в письме
  4. Проверить, что вход выполнен

---

### 3. Тестирование основных функций

#### 3.1 Wallet Dashboard

- [ ] **Главная страница:**
  - [ ] Все секции отображаются корректно
  - [ ] Token balance card работает
  - [ ] Price ticker обновляется
  - [ ] Tax card показывает комиссию
  - [ ] Dexscreener chart загружается
  - [ ] Wallet statistics отображаются

- [ ] **Мультиязычность:**
  - [ ] Переключение языка (RU/EN/LT/LV) работает
  - [ ] Все тексты переведены
  - [ ] Язык сохраняется при перезагрузке страницы

#### 3.2 Exchange System

- [ ] **Калькулятор обмена:**
  1. Ввести сумму токенов
  2. Проверить, что RUB эквивалент рассчитан
  3. Проверить комиссию

- [ ] **Создание заявки на обмен:**
  1. Заполнить форму (wallet address, email, комментарий)
  2. Нажать "Отправить заявку"
  3. Проверить toast уведомление об успехе
  4. Проверить в БД, что заявка создана:
     ```bash
     docker compose -f docker/docker-compose.prod.yml exec postgres psql -U postgres -d web_wallet_db -c "SELECT * FROM exchange_requests ORDER BY created_at DESC LIMIT 5;"
     ```

- [ ] **Проверка уведомлений:**
  - [ ] Админ получил уведомление в Telegram
  - [ ] Email отправлен (если настроен)

#### 3.3 Internal Requests

- [ ] **Создание внутренней заявки:**
  1. Заполнить форму (requester, email, department, request_type, priority, description)
  2. Отправить заявку
  3. Проверить уведомление
  4. Проверить в БД

- [ ] **Просмотр заявок:**
  - [ ] Профиль пользователя показывает все заявки
  - [ ] Фильтрация по статусам работает
  - [ ] Сортировка работает

---

### 4. Тестирование Telegram интеграции

#### 4.1 Telegram Bot Commands

- [ ] **`/start`** — приветствие и список команд
- [ ] **`/list`** — показать все заявки
- [ ] **`/exchange`** — показать заявки на обмен
- [ ] **`/internal`** — показать внутренние заявки
- [ ] **`/details <ID>`** — детали конкретной заявки

#### 4.2 Inline Buttons

Для каждой заявки должны быть кнопки:

- [ ] 🔄 **Обработать** — меняет статус на `processing`
- [ ] ✅ **Завершить** — меняет статус на `completed`
- [ ] ❌ **Отклонить** — меняет статус на `rejected`
- [ ] 🚫 **Отменить** — меняет статус на `cancelled`

#### 4.3 Проверка изменений в БД

После каждого изменения статуса проверить:

```bash
docker compose -f docker/docker-compose.prod.yml exec postgres psql -U postgres -d web_wallet_db -c "SELECT id, status, updated_at FROM exchange_requests ORDER BY updated_at DESC LIMIT 5;"
```

---

### 5. Тестирование Support Messenger

#### 5.1 Отправка сообщения пользователем

- [ ] **Открыть страницу Support:**
  1. Перейти на `/support`
  2. Отображается интерфейс мессенджера

- [ ] **Отправить тестовое сообщение:**
  1. Ввести текст в поле ввода
  2. Нажать "Отправить"
  3. Проверить, что сообщение появилось в чате
  4. Проверить toast уведомление

- [ ] **Проверка в БД:**
  ```bash
  docker compose -f docker/docker-compose.prod.yml exec postgres psql -U postgres -d web_wallet_db -c "SELECT * FROM support_messages ORDER BY created_at DESC LIMIT 5;"
  ```

#### 5.2 Ответ админа через Telegram

- [ ] **Уведомление админу:**
  - [ ] Админ получил уведомление о новом сообщении
  - [ ] Уведомление содержит адрес кошелька
  - [ ] Уведомление содержит текст сообщения

- [ ] **Команды для ответа:**
  - [ ] **`/chats`** — список активных сессий
  - [ ] **Кнопка "💬 Ответить"** — активирует режим ответа
  - [ ] **Отправка текста** — отправляет сообщение пользователю

- [ ] **Получение ответа пользователем:**
  - [ ] Сообщение админа появилось в чате
  - [ ] Real-time обновление (без перезагрузки)
  - [ ] Индикатор "Админ печатает..." работает

#### 5.3 История чата

- [ ] **Просмотр истории:**
  - [ ] Кнопка "📜 История чата" в уведомлении Telegram
  - [ ] История отображается в Telegram
  - [ ] Форматирование читаемое
  - [ ] Возможность ответить сразу

#### 5.4 Typing Indicators

- [ ] **Отправка индикатора:**
  - [ ] Админ начинает печатать
  - [ ] Пользователь видит "Админ печатает..."
  - [ ] Индикатор исчезает через 30 секунд или после отправки

---

### 6. Тестирование Chatbot

#### 6.1 Автоматическое появление

- [ ] **Таймер:**
  - [ ] Чат появляется через 10 секунд после подключения кошелька
  - [ ] Появляется только один раз в сессии
  - [ ] Не появляется, если уже был показан

#### 6.2 Базовые ответы

- [ ] **Приветствие:**
  - [ ] Бот приветствует пользователя
  - [ ] Предлагает варианты вопросов

- [ ] **Автоответы:**
  - [ ] "Как подключить кошелек?"
  - [ ] "Где посмотреть баланс?"
  - [ ] "Как подать заявку?"

#### 6.3 Анализ транзакций

- [ ] **Отправка tx hash:**
  1. Отправить валидный tx hash
  2. Проверить, что бот анализирует транзакцию
  3. Проверить, что результаты отображаются

- [ ] **Обработка невалидных данных:**
  - [ ] Невалидный hash — понятное сообщение об ошибке
  - [ ] Tx hash не существует — соответствующее сообщение

#### 6.4 Перевод

- [ ] **Переключение языка:**
  - [ ] Кнопка перевода работает
  - [ ] Сообщения переводятся корректно
  - [ ] Перевод сохраняется

#### 6.5 Эскалация к человеку

- [ ] **Триггеры эскалации:**
  - [ ] Слово "мошенничество"
  - [ ] Слово "помощь"
  - [ ] Вопрос требует человеческого ответа

- [ ] **Эскалация:**
  - [ ] Админ получает уведомление в Telegram
  - [ ] Пользователь видит, что сообщение передано админу
  - [ ] Реальный админ может ответить

---

### 7. Тестирование на мобильных устройствах

#### 7.1 Адаптивность

- [ ] **iPhone/Safari:**
  - [ ] Все страницы отображаются корректно
  - [ ] Навигация работает
  - [ ] Формы удобны для ввода

- [ ] **Android/Chrome:**
  - [ ] Все страницы отображаются корректно
  - [ ] MetaMask подключение работает
  - [ ] Чат-бот работает

#### 7.2 Mobile-specific функции

- [ ] **Touch interactions:**
  - [ ] Кнопки достаточно большие
  - [ ] Свайпы работают (если есть)

- [ ] **MetaMask Mobile:**
  - [ ] Подключение через MetaMask Mobile
  - [ ] Подписание транзакций (если есть)

---

### 8. Проверка производительности

#### 8.1 Lighthouse Test

```bash
# Установить Lighthouse CLI
npm install -g lighthouse

# Запустить тест
lighthouse http://localhost:3000 --view
```

**Целевые показатели:**

- Performance: ≥ 80
- Accessibility: ≥ 90
- Best Practices: ≥ 90
- SEO: ≥ 90

#### 8.2 Bundle Size

- [ ] **Проверить размер bundle:**

  ```bash
  npm run build
  # Посмотреть размер .next в выводе
  ```

- [ ] **Целевой размер:**
  - Главная страница: < 250KB (gzipped)
  - Отдельные страницы: < 150KB (gzipped)

#### 8.3 Load Time

- [ ] **Time to Interactive (TTI):** < 3 сек
- [ ] **First Contentful Paint (FCP):** < 1.5 сек
- [ ] **Largest Contentful Paint (LCP):** < 2.5 сек

---

### 9. Проверка ошибок

#### 9.1 Console Errors

- [ ] **Открыть DevTools → Console**
- [ ] Убедиться, что нет критических ошибок
- [ ] Единственные warning'и — для несуществующих файлов (notification-bell.mp3)

#### 9.2 Network Errors

- [ ] **Открыть DevTools → Network**
- [ ] Проверить, что все запросы возвращают 200/307 статусы
- [ ] Нет 404/500 ошибок

#### 9.3 Database Errors

- [ ] **Проверить логи:**
  ```bash
  docker compose -f docker/docker-compose.prod.yml logs postgres --since 1h | grep -i "error" | wc -l
  ```
  Должно быть: 0 или минимальное количество старых ошибок

---

### 10. Финальная проверка

#### 10.1 Общий Checklist

- [ ] Все основные функции работают
- [ ] Нет критических ошибок в логах
- [ ] Performance соответствует требованиям
- [ ] Мобильная версия работает
- [ ] Multilingual работает
- [ ] Интеграции (Telegram, Email) работают
- [ ] Безопасность настроена (CSP, headers)

#### 10.2 Документация

- [ ] Прочитать все чеклисты в `docs/deployment-readiness.md`
- [ ] Убедиться, что все переменные окружения задокументированы
- [ ] Проверить, что инструкции по деплою актуальны

---

## 🐛 Troubleshooting

### Проблема: Ошибки в логах БД

**Решение:**

1. Проверить, что все миграции применены
2. Пересоздать БД при необходимости:
   ```bash
   docker compose -f docker/docker-compose.prod.yml down -v
   docker compose -f docker/docker-compose.prod.yml up --build
   ```

### Проблема: CSP violations

**Решение:**

1. Проверить `next.config.ts`
2. Убедиться, что все домены добавлены в `connect-src`
3. Пересобрать приложение: `npm run build`

### Проблема: Telegram бот не отвечает

**Решение:**

1. Проверить `TELEGRAM_API_KEY` в переменных окружения
2. Проверить webhook:
   ```bash
   curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo
   ```
3. Проверить логи:
   ```bash
   docker compose -f docker/docker-compose.prod.yml logs web | grep telegram
   ```

### Проблема: OAuth не работает

**Решение:**

1. Проверить `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
2. Проверить `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`
3. Проверить `AUTH_SECRET`
4. Проверить `NEXTAUTH_URL`

---

## 📊 Результаты тестирования

После выполнения всех тестов заполните таблицу:

| Функция              | Статус | Примечания |
| -------------------- | ------ | ---------- |
| Wallet Connection    | ✅/❌  |            |
| OAuth Auth           | ✅/❌  |            |
| Exchange Calculator  | ✅/❌  |            |
| Telegram Integration | ✅/❌  |            |
| Support Messenger    | ✅/❌  |            |
| Chatbot              | ✅/❌  |            |
| Multilingual         | ✅/❌  |            |
| Mobile Responsive    | ✅/❌  |            |
| Performance          | ✅/❌  |            |
| Security             | ✅/❌  |            |

---

## ✅ Готовность к Production

**Критерии готовности:**

- [ ] Все тесты из разделов 1-9 пройдены
- [ ] Нет критических ошибок в логах
- [ ] Performance ≥ 80
- [ ] Все переменные окружения настроены
- [ ] Telegram webhook работает
- [ ] Бэкапы БД настроены
- [ ] Мониторинг настроен (или планируется)

**Если все чеклисты выполнены — можно деплоить в production!**

---

**Последнее обновление:** 31 октября 2025  
**Версия:** 1.0
